<div class='jumbotron'>
  <h2>Foodspot is the easiest way to find and reviews on local restaurant </h2>
  <h3>Explore your favorite food on the go <br> leave reviews and rating for your fav foodies</h3>
</div>
<h1>Listing food_places</h1>

<table class='table'>
  <thead>
    <tr>
      <th>Name</th>
      <th>Address</th>
      <th>Phone</th>
      <th>Website</th>
      <% if user_signed_in? && current_user.admin? %>
        <th colspan="2"></th>
      <% end %>
    </tr>
  </thead>

  <tbody>
    <% @food_places.each do |food_place| %>
      <tr>
        <td><%= link_to food_place.name, food_place %></td>
        <td><%= food_place.address %></td>
        <td><%= food_place.phone %></td>
        <td><%= link_to food_place.website, food_place.website, target:'_blank' %></td>
        <!-- -->
        <% if user_signed_in? && current_user.admin? %>
          <td><%= link_to 'Edit', edit_food_place_path(food_place), class: "btn btn-link" %></td>
          <td><%= link_to 'Destroy', food_place, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-link" %></td>
        <% end %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

