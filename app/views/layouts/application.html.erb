<!DOCTYPE html>
<html>
<head>
  
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "FoodPl.us: Add and Search for local restaurant" %></title>
    <meta name="keywords" content="<%= content_for?(:keywords) ? yield(:keywords) : "restaurant reviews, restaurant app, restaurant locator, restaurant search, food finder, restaurant listings" %>">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "All inclusive site for restaurant. Find, search and list restaurant web app. " %>">
    <meta name="author" content="Said Maadan">
  <%= render 'layouts/bootstrap'%>  
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
  <%= favicon_link_tag 'https://s3.amazonaws.com/inventivelabs/jobinbox/logos/favicon5.ico' %>
</head>
<body>
<%= render 'layouts/navbar'%>
<section class='section'>

	<% flash.each do |name, msg| %>
		<% if msg.is_a?(String) %>
			<div class="alert alert-<%= name = "notice" ? "info" : "danger" %> alert-dismissable">
				<button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
				<%= content_tag :div, msg, :id => "flash_#{name}" %>
				</div>
		<% end %>
	<% end %>

	<%= yield %>

</section>
<div class='push'></div>
<%= render 'layouts/footer'%>

</body>
</html>
